# M3 开发试跑：停止扩大调用

> 2026-08-28 追加更正：下方原始观察中关于 s002/s003“源码片段过度转义/不一致”的描述错误，已撤回；保留原文以便追溯。JSON解码后的全部8处snippet均与对应源代码行一致。详见文末更正与人工复核记录。

2026-08-27，用户确认账户至少可用300000 token且不会额度外扣费，并自愿承担人工审核。
此为用户确认，不伪称独立查询到账户余额。使用代码检查点f28f384，材料/Prompt未改动。

## 实际结果

| 样本 | 类型 | HTTP | 严格解析 | 输入/输出/总token | 耗时ms |
| --- | --- | --- | --- | --- | --- |
| s001 | 正确代码 | 200 | schema_invalid | 2052 / 9720 / 11772 | 88710 |
| s002 | 比较符边界错误 | 200 | schema_invalid | 2052 / 8993 / 11045 | 85697 |
| s003 | 贪心策略错误 | 200 | schema_invalid | 2050 / 11553 / 13603 | 99584 |

总计3次请求，无重试；输入6154、输出30266、总计36420 token。
未知消耗0，剩余预算263580 token，剩余次数上限35。预算余量不代表需要继续消耗。
每次请求前持久化214016 token保守预留；返回后usage一致性核对成功。

三条均为开发试跑，不进入正式指标；HTTP成功3/3、严格契约通过0/3、可用完整源码0/3。
正式样本纳入0，不能得出候选诊断准确率、完整解答准确率、定位准确率或误报率。

## 失败观察与结论

- s001：正文仅为空键/空值对象，缺少全部必需诊断字段。
- s002：部分诊断内容被错误嵌入steps数组，缺少完整solution_code，部分源码片段过度转义。
- s003：counterexample/reference_solution处于错误层级，缺少solution_code，源码片段也有转义不一致。
- s002/s003原文提到相应比较符/排序问题，但响应整体不合法，只作定性观察，不能算成功诊断。
- 未修改raw、未自动补字段、未剥fence、未降低校验要求。
- 现有记录没有保存provider finish_reason或推理token细分，不能据此确定生成异常的服务端根因。
  总输出usage均低于16384，不能武断认定是输出上限截断。

按授权中的稳定性门槛，停止扩大真实调用，不冻结正式实验、不进入保留集。
没有发现足以证明必须修复的本地请求构造bug；不为筛选结果自动消耗额外3次验证额度。
后续若决定修改输出契约，应另存新版本，先做离线测试，再明确记录新增验证而非重试。

## 证据与审核

脱敏用量、版本和raw SHA清单：[development-pilot-summary.json](../../evaluation/results/development-pilot-summary.json)。
完整原始响应及请求仅在Git忽略目录build/m3-development-20260827/下，每条各有
request.json、raw-response.txt、record.json；budget/保留reserve/done，不删除、不重置。
三个raw SHA与记录一致。精确Key及Authorization/Bearer检查未发现泄漏。
本轮没有执行模型代码、没有候选代码执行、没有OJ调用。

人工审核人已分配为项目作者，但实际审查仍pending。请先检查：
1. 材料gold的正确/边界/策略标签是否成立；不要根据Hy3响应反改gold。
2. 本次结构失败分类及定性观察是否准确。
3. 是否值得做一版更明确的独立输出契约；不可把修正后的结果覆盖本次失败。

源码、模型配置、冻结文件未改动；仅新增脱敏记录及状态文档，因此不重复构建/CTest/CI。
此前CI33084266081仍是代码验证证据，不声称本次文档补记重新跑了CI。

## 2026-08-28 追加：人工复核与错误描述更正

项目作者已提交 s001–s003 的实际人工审核：检查题面、代码、gold、实际请求和原始响应。三条gold成立，不依赖Hy3判断，不修改gold；其余22条未在此次审核范围内。

- s001：最大的k项之和不小于任何k项之和；降序前缀首次严格超过总和一半，因此件数最少。
- s002：第9行边界错误成立。输入 `2 / 5 5` 应输出2，`2*s>=sum` 在一项时提前停止，静态推断输出1。
- s003：第8行策略错误成立。输入 `3 / 8 1 1`，只选8即可；升序累加会选满三项。

三份原始响应均为合法JSON，SHA与原记录一致；失败是 `schema_invalid`，不是JSON语法错误。s001缺必需字段，s002字段嵌入steps且出现字符串solution_code，s003字段处于错误顶层且缺solution_code。

**撤回片段转义错误的原始描述。** 人工审核及Planner离线JSON解码检查确认：s002/s003共8处 `code_location.snippet` 全部逐字匹配声明行范围；此前把JSON文本转义当作解码后错误，是记录判断失误。此更正不改变三次结构失败分类。

s002/s003原文识别到相关根因只能作为定性观察，不能算通过契约的成功诊断。旧记录缺finish_reason，不能确定截断或服务端异常原因。

原Prompt先给交互“唯一契约”再追加评测封装，并含非JSON占位示例，是明确的歧义风险，但尚不能证明就是失败根因。独立 [evaluation-v2](../evaluation-v2.md) 仅先离线验证；三次失败、原raw、SHA和36420 token不变，不覆盖、不重试、不重置账本。

审核证据：[s001–s003记录](../../evaluation/reviews/s001-s003-20260828.json)。记录日期为入库日期，不推断用户实际审核时刻。本轮没有新增模型调用。
